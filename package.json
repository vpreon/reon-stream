{
  "name": "reon-stream",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply reon-stream-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply reon-stream-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
